# ANKUZO — the personal archive, published through the host's existing edge.
#
# Deliberately publishes no ports. Caddy reaches this container by name over
# the shared `edge` network on port 3000.
services:
  ankuzo:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    image: ankuzo:latest
    container_name: ankuzo
    restart: unless-stopped
    environment:
      TZ: ${TZ:-UTC}
    volumes:
      - ${ANKUZO_DATA:-/opt/ankuzo-data}:/usr/share/nginx/html/data:ro
    networks: [edge]
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "5"
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s

networks:
  edge:
    name: edge
    external: true
